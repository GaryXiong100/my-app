sudo: required 
language: node_js
node_js:
    - 16
    
services: 
    - docker
before_install: 
    - docker build -t garyxiong100dd/sample-app -f Dockerfile.dev .
script:     
    - docker run -e CI=true garyxiong100dd/sample-app npm run test -- --coverage --watchAll=false
